from setuptools import setup, find_packages

setup(
    name="src", 
    version="0.0.1", 
    description="its a mlops package for online shoppers intention",
    author="vikram",
    packages=find_packages(),
    license="Apache"
)
